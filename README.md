# BrookPay

Multi-currency wallet backend. Python >= 3.10, standard library only.

## Task

> The structured return value of `get_user_balance` (in
> `brookpay/services/account_service.py`) is too heavy for our use case.
> Simplify this function so it returns the account's numeric amount
> directly. You may modify any other file in the project if needed.
> Provide the full content of every modified file.
